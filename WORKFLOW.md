# 开发流程：一律先开工作树

主分支 `main` 任何时候都保持「能跑、测试全过」的状态。所有改动都在独立的
git 工作树里做，测试通过后才合并回 `main` 并推送远端。

这样做的目的：改动过程中崩了、改坏了、或者跑测试把环境弄乱了，都只影响那块
隔离的目录，`main` 不受牵连，随时可以重来。

## 三个脚本

| 脚本 | 作用 |
| --- | --- |
| `scripts/wt-new.sh <分支名>` | 从最新 `origin/main` 开一个隔离工作树，路径为 `.worktrees/<分支名，斜杠换成短横线>` |
| `scripts/wt-check.sh [路径]` | 跑三个测试（核心逻辑 / 界面冒烟 / 真实终端启动），任一失败即返回非零 |
| `scripts/wt-ship.sh <分支名> "<说明>"` | 提交 → 对齐远端 → 全量测试 → 快进合并 `main` → 推送 → 清理工作树 |

`.worktrees/` 已在 `.gitignore` 中忽略，不会污染仓库内容。

## 标准步骤

```bash
# 1. 开工作树
scripts/wt-new.sh feature/xxx
cd .worktrees/feature-xxx        # 分支名的斜杠换成短横线

# 2. 在这里改代码（随便折腾）

# 3. 先自测
scripts/wt-check.sh

# 4. 测试过了再收口：合并到 main 并推送
cd ../..
scripts/wt-ship.sh feature/xxx "一句话说明这次改了什么"
```

## 约定

- **测试必须全绿才合并。** `wt-ship.sh` 里测试是硬门槛，失败会直接中止，
  工作树原样保留，可以原地改到通过再重跑。
- **合并用 `--ff-only`。** 如果 `main` 已经往前走（远端有新提交），脚本会先把
  工作树 rebase 到最新再测一遍，避免把没测过的合并结果推上去。
- **失败不清场。** 只有走完全部步骤才删除工作树；中途失败一律保留，方便排查。
- 临时分支在合并后自动删除。要放弃某个工作树，用
  `git worktree remove .worktrees/<目录名> --force` + `git branch -D <分支名>`。
